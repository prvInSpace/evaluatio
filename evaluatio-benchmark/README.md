# evaluatio-benchmark

This component is responsible to ensuring that benchmarking is transparent and reproducible.

## How to use

To run a specific benchmark you pass it as an argument when you run the script like so:
```bash
uv run evaluatio-benchmark --config metrics.wer.wer_benchmark
```